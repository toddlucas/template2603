import type { SidebarNavMainItem } from '$/features/frame/components/sidebar-types';

// The onboarding app uses a full-screen layout and doesn't use the standard sidebar.
// This stub satisfies the AppConfig interface.
export const onboardingSidebarData: SidebarNavMainItem[] = [];
